# StudyConnect

This project now integrates `streamlit-shadcn-ui` for a more modern
component library. Install the dependency along with the others:

```bash
pip install -r requirements.txt
```

Components such as `Button`, `Card`, and `Input` are used when the
package is available; the app falls back to basic Streamlit widgets if
not.
